const tail: [_] -> [_] = |_| [];

const len: [_] -> Int = |l|
	if l = []
	then 0
	else 1 + len(tail(l));

# Inferred to [Int]
const my_list: [_] = [x, y, z, [a, b, 3]];

# Inferred to Int
const five = (|x| 5)(0);

# Inferred to Bool
const my_bool = x or (y and !z or false);
